# Specification - Project Renaming

Rename the project and all its components from `hydra-which` (typo) to `hydra-which`.

## Objectives
- Rename GitHub repository from `hydra-which.nvim` to `hydra-which.nvim`.
- Update local git remote.
- Rename directories and files containing `hydra-which`.
- Update all occurrences of `hydra-which` and `hydra-which` in the codebase and documentation to `hydra-which`.
- Ensure consistency in the plugin name `hydra-which.nvim`.

## Scope
- `lua/hydra-which/` -> `lua/hydra-which/`
- `doc/hydra-which.txt` -> `doc/hydra-which.txt`
- All files in `conductor/`
- `README.md`, `README_zh.md`
- `examples/`
- `.gitignore`, `LICENSE`
- Git remote configuration.
